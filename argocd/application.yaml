apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chatbot
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/woodez-kubernetes/chatbot.git
    targetRevision: release-dev
    path: helm/chatbot


    helm:
      releaseName: chatbot

      valueFiles:
        - values.yaml
      
      values: |
        image:
          respository: kwood475/chatbot
          # tag is controlled by values.yaml (updated by CI/CD workflow)
       
        namespace: monitoring

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    # automated:
    #  prune: true
    #  selfHeal: true
    #  allowEmpty: false

    syncOptions:
      # Create namespace if it doesn't exist
      - CreateNamespace=true
      # Respect ignore differences
      - RespectIgnoreDifferences=true
      # Server-side apply
      - ServerSideApply=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m