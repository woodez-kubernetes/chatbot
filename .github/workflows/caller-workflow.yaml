name: Sequential Workflow Calls
on:
  push:
    branches:
      - release-dev

jobs:
  # 1. First reusable workflow - Build and push Docker image
  build-code:
    uses: ./.github/workflows/docker-build-publish.yaml
    permissions:
      contents: write
      packages: write
    secrets: inherit

  # 2. Second reusable workflow, runs ONLY after 'build-code' succeeds
  ##run-tests:
  ##  needs: build-code
  ##  uses: ./.github/workflows/reusable-test.yml
  ##  secrets: inherit

  # 3. Third reusable workflow, runs ONLY after 'run-tests' succeeds
  ##deploy-app:
  ##  needs: run-tests
  ##  uses: ./.github/workflows/reusable-deploy.yml
  ##  secrets: inherit